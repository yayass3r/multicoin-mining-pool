# =====================================================
# 🚀 Zeet.app Configuration
# =====================================================

name: multicoin-mining-pool

build:
  dockerfile: Dockerfile

deploy:
  ports:
    - 10000
    - 3333
    - 3334
    - 3336
  health:
    port: 10000
    path: /api/health
  env:
    NODE_ENV: production
    PORT: "10000"
